<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usedcar.dashboard.mapper.ShareLinkMapper">

    <resultMap id="ShareLinkResultMap" type="com.usedcar.dashboard.entity.ShareLink">
        <id column="id" property="id"/>
        <result column="token" property="token"/>
        <result column="view_id" property="viewId"/>
        <result column="filters_json" property="filtersJson"
                typeHandler="com.usedcar.dashboard.handler.JacksonTypeHandler"
                javaType="com.usedcar.dashboard.dto.DashboardFilter"/>
        <result column="permissions_json" property="permissionsJson"
                typeHandler="com.usedcar.dashboard.handler.JacksonTypeHandler"
                javaType="java.util.List"/>
        <result column="creator" property="creator"/>
        <result column="includes_turnover_metrics" property="includesTurnoverMetrics"/>
        <result column="is_valid" property="isValid"/>
        <result column="created_at" property="createdAt"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="revoked_at" property="revokedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, token, view_id, filters_json, permissions_json, creator,
        includes_turnover_metrics, is_valid, created_at, expires_at, revoked_at
    </sql>

    <select id="selectByToken" resultMap="ShareLinkResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_share_link
        WHERE token = #{token}
          AND is_valid = 1
          AND revoked_at IS NULL
          AND expires_at > NOW()
    </select>

    <insert id="insert" parameterType="com.usedcar.dashboard.entity.ShareLink" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_share_link (
            token, view_id, filters_json, permissions_json, creator,
            includes_turnover_metrics, is_valid, created_at, expires_at, revoked_at
        ) VALUES (
            #{token}, #{viewId},
            #{filtersJson, typeHandler=com.usedcar.dashboard.handler.JacksonTypeHandler, javaType=com.usedcar.dashboard.dto.DashboardFilter},
            #{permissionsJson, typeHandler=com.usedcar.dashboard.handler.JacksonTypeHandler, javaType=java.util.List},
            #{creator}, #{includesTurnoverMetrics}, #{isValid},
            #{createdAt}, #{expiresAt}, #{revokedAt}
        )
    </insert>

    <update id="revokeByToken">
        UPDATE t_share_link
        SET is_valid = 0,
            revoked_at = NOW(),
            expires_at = LEAST(expires_at, NOW())
        WHERE token = #{token}
          AND is_valid = 1
    </update>

</mapper>
